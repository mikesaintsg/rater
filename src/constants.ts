/**
 * @mikesaintsg/rater
 *
 * Constants for the rater library.
 */

/** Default base rate */
export const DEFAULT_BASE_RATE = 0

/** Default decimal places for rounding */
export const DEFAULT_DECIMAL_PLACES = 2

/** Default continue on error setting */
export const DEFAULT_CONTINUE_ON_ERROR = true

/** Default group aggregation method */
export const DEFAULT_GROUP_AGGREGATION_METHOD = 'sum' as const
